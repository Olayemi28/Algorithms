using System;

namespace Question_3
{
    class Program
    {
        static void Main(string[] args)
        {
           // 3.	(Random character) Write a program that displays a random uppercase letter using the Math.random() method.
           Random random = new Random();
           int rand = random.Next();
        }
    }
}
